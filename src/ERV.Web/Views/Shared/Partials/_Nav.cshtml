
@{
    var language = (string)ViewBag.CultureTwoLetterName;
    var languageName = language == "ES" ? Lang("_English") : Lang("_Spanish");
    var newLanguage = language == "ES" ? "EN" : "ES";
}

<header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">ERV.Web</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

@*<nav class="site-nav">
    <div class="container">
        <div class="menu-bg-wrap">
            <div class="site-navigation">
                <a asp-controller="Home" asp-action="Index" class="logo m-0 float-start">Countries</a>
                <ul class="js-clone-nav d-none d-lg-inline-block text-start site-menu float-end">
                    <li class="active"><a asp-controller="Home" asp-action="Index">@Lang("_Home")</a></li>
                    <li><a asp-controller="Home" asp-action="Ontology">All Countries</a></li>
                    <li class="language-container">
                        <a class="change-lang" href="#" value="@newLanguage">@languageName</a>
                    </li>
                </ul>
                <a href="#" class="burger light me-auto float-end mt-1 site-menu-toggle js-menu-toggle d-inline-block d-lg-none" data-toggle="collapse" data-target="#main-navbar">
                    <span></span>
                </a>
            </div>
        </div>
    </div>
</nav>*@

@Html.Script(@<script type="text/javascript">
            $('.language-container .change-lang').on('click', function(e) {
                var lang = $(this).attr('value');
                if (lang !== '') {
                    e.preventDefault();
                    countryApp.Ajax.Post('@Url.Action("ChangeLanguage", "Home")', { returnUrl: '@Context.Request.GetEncodedUrl()', newLang: lang },
                        function(datos) {
                            window.location.href = datos.targetUrl.url;
                        },
                        function(datos) { }
                    )
                }
            });
    </script>)